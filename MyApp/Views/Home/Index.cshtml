@model IEnumerable<MyApp.Models.Creative>
@{
    ViewData["Title"] = "Home";
}
<h2>ViewData["Title"]</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>Name</th>
        <th>Content</th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
        <td>@item.Name</td>
        <td>@item.Content</td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.Name">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.Name">Task</a> |
            <a asp-action="Delete" asp-route-id="@item.Name">Delete</a>
        </td>
    </tr>
    }
</table>